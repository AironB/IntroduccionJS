//objeto math

let resultado;

resultado = Math.PI
resultado = Math.round(2.2);


//Metodo para redondear hacia arriba
resultado = Math.ceil(2.2); 

//Metodo para redondear hacia abajo
resultado = Math.floor(2.1);
//Raiz cuadrada
resultado = Math.sqrt(144);
//Metodo para convertir numeros negativos a positivos
resultado = Math.abs(-200);
//Metodo para saber cual es el numero menor de un listado
resultado = Math.min(3, 5, 1, 8, 2, 10);
//Metodo para mostrar el numero mayor de un listado
resultado = Math.max(3, 5, 1, 8, 2, 10);
//Generar numero aleatorio
resultado = Math.random();
resultado = Math.floor( Math.random() * 30);





console.log(resultado);

